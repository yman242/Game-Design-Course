title = argument0; // Tip or Hint or Information
display_message = argument1; // Message to display
duration = argument2; // duration to display it

temp_id = instance_create(0,0,obj_achievements);
temp_id.title = title;
temp_id.display_message = display_message;
temp_id.duration = duration;
